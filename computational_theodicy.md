# Computational Theodicy
## Scope
## Misc. Notes
## Analogies and Hierarchies

### Judgement Function
``` python
def Classify(deed,faith,knowledge,laws_id, alpha):
  import math
  import pandas as pd
  def deed(strate:float = [0,1],conciousness:float,impact:int = [0, math.inf]):
    from skfuzzy import
    strate = #fuzzy
    conciousness =
    impact = count()
    return
  def faith(prime:bool,seconde:bool,tertie:bool,quarte:bool,quinte:bool,sexte:bool):
      sqlalchemy.query(pass)
      pass
  laws_id = sqlalchemy.query(pass) #int primary key
  knowledge = pass #information retrieval vs available module to the deeds node
  alpha = float(input("God's Prerogative Valuing: ")
  return (pass)
```
